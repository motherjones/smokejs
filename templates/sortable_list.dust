<ul data-attribute="{attribute}"
    data-slug="{slug}"
    data-template="{template}" >
  {#list temp=template}
    <li data-slug="{.slug}">
        {#render template=temp /}
        {@sep}{delimiter}{/sep}
    </li>
  {/list}
</ul>
